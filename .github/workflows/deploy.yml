name: Deploy do Brazil Instrumentarium no GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout do Repositório
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: web/package.json

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: 'pip'

      - name: Instalar Dependências Python
        run: pip install -r requirements.txt

      - name: Executar Testes Python (pytest)
        run: pytest

      - name: Instalar Dependências (Strict Lockfile)
        run: cd web && npm ci

      - name: Sincronizar Conteúdo
        run: python3 scripts/sync_web_content.py

      - name: Executar Testes Unitários Web (Pré-Build)
        run: cd web && npm test

      - name: Build do Portal Astro e Pagefind
        run: cd web && npm run build

      - name: Validar Busca Pagefind e Páginas Compiladas (Pós-Build)
        run: cd web && npm test

      - name: Upload do Artefato
        uses: actions/upload-pages-artifact@v3
        with:
          path: web/dist

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy no GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
